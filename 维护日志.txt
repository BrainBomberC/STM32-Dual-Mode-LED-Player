2026.1.6 采用stm32f412rgt6芯片，采用cubemx+vscode开发程序，根目录含有通用cmakelists.txt，可参考复用
1、配置了定时器5，通道1，2，3，4分别由PWM+DMA控制，引入了led_driver2，每个口可以同时输出PWM波（基于归零码）；
2、搭建了tf卡，并且部署了FATFS文件操作系统；
3、开启了按键中断PC13；
4、开启了三个gpio控制三个led；
5、开启了串口中断1；

以上测试均正常。


2026.1.16 已经完成了usb插入读写tf卡功能，写好了状态机制；


2026.1.19 准备部署led屏幕进行状态显示

2026.1.21 完成了lcd屏幕显示（横屏显示）、
2026.1.21 完成了usb状态机制、tf卡读取机制两者的lcd显示

2026.1.25 完成了6通道输出PWM的配置，其中通道1-4作为内置IC的输入控制口，5、6作为sm15155的输出控制口（裸机开发）